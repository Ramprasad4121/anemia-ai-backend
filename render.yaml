services:
  - type: web
    name: anemia-api
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app_combined:app
    envVars:
      - key: FLASK_ENV
        value: production
    autoDeploy: true